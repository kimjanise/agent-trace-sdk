[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-trace-sdk"
version = "0.1.0"
description = "SDK for tracing AI agent executions"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["agent_trace"]